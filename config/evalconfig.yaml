# @package _global_

defaults:
  - private: private.yaml
  - _self_

checkpoint: ???   # required: path to .ckpt file

data:
  path: null              # single H5 file; takes precedence over data_dir
  data_dir: data/gridsearch

n_samples: 3

wandb:
  project: mich-bold-inversion
  entity: ${private.entity}
  run_name: null
